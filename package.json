{
  "name": "test",
  "version": "0.0.1",
  "workspaces": [
    "packages/utils",
    "packages/core"
  ],
  "scripts": {
    "build": "npm run build --workspaces"
  },
  "devDependencies": {
    "rimraf": "^5.0.7",
    "typescript": "^5.4.5"
  }
}
